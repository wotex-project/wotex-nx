# Agent Instructions

Read and follow `CLAUDE.md` before repository work. Apply matching rules and
skills under `.claude/`. Keep this library consumer-neutral and locally verify
all gates before a commit. Never push unless a human explicitly requests it.
