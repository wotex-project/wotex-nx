{
  "skip_files": ["test/support/.*"],
  "skip_file_reasons": {
    "test/support/.*": "Test-only factories and ports are compiled through test elixirc_paths and are not runtime or public API coverage surfaces."
  },
  "coverage_options": {
    "minimum_coverage": 95
  }
}

